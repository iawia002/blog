<section class="text-base">
  <ul>
    {% for post in postslist | reverse %}
      <li class="mb-2">
        <span class="text-gray-500 mr-2">{% formatDate post.date %}</span>
        <a href="{{ post.url }}">{{ post.data.title }}</a>
      </li>
    {% endfor %}
  </ul>
</section>
