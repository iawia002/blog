<section>
  <ul>
    {% for post in postslist | reverse %}
      <li class="flex mb-2">
        <span class="flex-none w-24 mr-1 text-gray-500">{% formatDate post.date %}</span>
        <a href="{{ post.url }}">{{ post.data.title }}</a>
      </li>
    {% endfor %}
  </ul>
</section>
